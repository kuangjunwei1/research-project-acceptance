# 文件命名和版本管理

采用用户指定规范；未给规范时可建议“项目名称-文件类型-完成单位”，但不要声称是强制制度。

若平台要求模块号和文件号，先读取当前模块映射，再生成序号。模块号不由本技能预设。同一材料的Word/PDF使用一致的主文件名。

报告名称体现真实用途、单位和承接范围。审计、决算、预算调整证明、原机构报告和划转部分报告不能混名。合同后缀用甲方还是乙方，按用户平台规则核查。

修改前生成名称映射并检查目标是否已占用；重复编号可能是合法格式配对，也可能是两份不同材料，需要核对内容。正式文件只有可辨认的有效最终版本；备份和暂不提交的源材料留在正式目录外。
